
ssh-keygen -t rsa -C "chengyeliu@g.harvard.edu"

eval `ssh-agent`

ssh-add id_rsa

ssh-add -l

cat ~/.ssh/id_rsa.pub

ssh -T git@github.com

git remote set-url origin git@github.com:flagdream/CS703-1901-OGE-Student.git
